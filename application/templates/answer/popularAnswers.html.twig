{% extends 'base.html.twig' %}

{% block title %}Popular answers{% endblock %}

{% block body %}
    <div class="container my-myd-4">
        <div class="row">
            <h1>Most Popular Answers</h1>

            <ul class="list-unstyled">
                {% for answer in answers %}
                    {{ include('answer/_answer.html.twig', {
                        showQuestion: true
                    }) }}
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}